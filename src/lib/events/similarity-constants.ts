/** Minimum pg_trgm similarity score for event name (0–1). */
export const NAME_SIMILARITY_THRESHOLD = 0.45;

/** Maximum distance in meters for coordinate-based location match. */
export const COORD_PROXIMITY_METERS = 100;
