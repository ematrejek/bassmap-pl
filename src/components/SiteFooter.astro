---
import SiteFooterNav from "@/components/SiteFooterNav.astro";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<footer class:list={["border-border/70 border-t py-4", className]} aria-label="Stopka serwisu">
  <SiteFooterNav />
</footer>
