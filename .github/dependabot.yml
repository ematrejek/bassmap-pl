version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    # Version-update PRs kept failing CI (Astro 7, Zod 4, ESLint 10, etc.).
    # Security advisories still surface via GitHub Dependabot alerts.
    open-pull-requests-limit: 0
